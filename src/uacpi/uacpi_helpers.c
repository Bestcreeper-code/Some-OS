#include "uacpi_helpers.h"

#include "Logger.h"
#include "err_codes.h"
#include "event.h"
#include "uacpi.h"
int acpi_init() {
    
    // uacpi_status ret = uacpi_initialize(0);
    // if (uacpi_unlikely_error(ret)) {
    //     Sys_Error("uacpi_initialize error: %s", uacpi_status_to_string(ret));
    //     return -E_NODEV;
    // }

    // ret = uacpi_namespace_load();
    // if (uacpi_unlikely_error(ret)) {
    //     Sys_Error("uacpi_namespace_load error: %s", uacpi_status_to_string(ret));
    //     return -E_NODEV;
    // }

    
    // ret = uacpi_namespace_initialize();
    // if (uacpi_unlikely_error(ret)) {
    //     Sys_Error("uacpi_namespace_initialize error: %s", uacpi_status_to_string(ret));
    //     return -E_NODEV;
    // }

    

    
    return 0;
}
